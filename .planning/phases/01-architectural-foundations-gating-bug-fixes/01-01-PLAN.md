---
phase: 01-architectural-foundations-gating-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/shared/messages.ts
  - packages/server/src/shared/messages.frozen-v1.8.test.ts
  - packages/server/src/shared/messages.frozen-v1.9.test.ts
  - packages/server/src/shared/messages.frozen-v1.10.test.ts
  - tools/lint/deprecated-annotation.ts
  - tools/lint/deprecated-annotation.test.ts
  - tools/lint/README.md
  - package.json
autonomous: true
requirements:
  - ARCH-02
tags:
  - schema-evolution
  - lint
  - testing
must_haves:
  truths:
    - "v1.8, v1.9, v1.10 frozen-fixture client requests still parse cleanly under the current daemon Zod schemas in CI"
    - "Today's daemon-emitted messages still parse cleanly under v1.8, v1.9, v1.10 client schema expectations (forward-compat)"
    - "The schema source file declares a `RESERVED_FIELDS` registry that is exported and importable"
    - "`@deprecated since= removeAfter=` annotations follow a documented convention enforced by a warn-level lint"
    - "CI fails if any frozen-fixture parse test is broken by a new schema change"
  artifacts:
    - path: "packages/server/src/shared/messages.ts"
      provides: "RESERVED_FIELDS export + ARCH-02 discipline header comment"
      contains: "export const RESERVED_FIELDS"
    - path: "packages/server/src/shared/messages.frozen-v1.8.test.ts"
      provides: "v1.8 wire-compat parse tests"
      contains: 'describe("v1.8 wire compatibility"'
    - path: "packages/server/src/shared/messages.frozen-v1.9.test.ts"
      provides: "v1.9 wire-compat parse tests"
      contains: 'describe("v1.9 wire compatibility"'
    - path: "packages/server/src/shared/messages.frozen-v1.10.test.ts"
      provides: "v1.10 wire-compat parse tests"
      contains: 'describe("v1.10 wire compatibility"'
    - path: "tools/lint/deprecated-annotation.ts"
      provides: "Warn-level lint script asserting @deprecated annotations include `since=` and `removeAfter=`"
      exports: ["lintDeprecatedAnnotations"]
  key_links:
    - from: "packages/server/src/shared/messages.frozen-v1.{8,9,10}.test.ts"
      to: "packages/server/src/shared/messages.ts"
      via: "Zod schema imports + .parse() against frozen fixtures"
      pattern: "from \"./messages\\.js\""
    - from: "package.json scripts.lint:schema"
      to: "tools/lint/deprecated-annotation.ts"
      via: "node script invocation"
      pattern: "tools/lint/deprecated-annotation"
---

<objective>
Lay the schema-evolution discipline foundation for v1.11. Export `RESERVED_FIELDS` from the schema source, add the `@deprecated since= removeAfter=` annotation convention enforced by a warn-only lint, and land three frozen-fixture parse-test files that pin v1.8 / v1.9 / v1.10 wire shapes against today's Zod schemas.

Purpose (per D-02 + ROADMAP P1 success criterion #2): these tests are the CI-blocking guardrail every subsequent carve commit (Plan 04) and every later schema addition runs against. Without them, the carve cannot start safely.

Output:

- `packages/server/src/shared/messages.ts` gains a `RESERVED_FIELDS` export and an ARCH-02 discipline header comment.
- Three new test files in `packages/server/src/shared/` covering each historical wire version.
- A standalone lint script under `tools/lint/` that warns on bad `@deprecated` annotations (warn-only this phase per D-10; promoted to error in Phase 5).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-CONTEXT.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Existing analog from packages/server/src/shared/messages.attachments.test.ts:1-50 -->

```typescript
// Test file shape (mirror this exactly for the three frozen-vX.Y files):
import { describe, expect, it } from "vitest";
import {
  AgentUpdateMessageSchema,
  CreateAgentRequestMessageSchema,
  // ...other schemas exercised
} from "./messages.js";

const V1_X_AGENT_UPDATE_FIXTURE = {
  /* frozen JSON snapshot */
} as const;

describe("v1.X wire compatibility", () => {
  it("v1.X daemon -> client agent_update parses with current schema", () => {
    const parsed = AgentUpdateMessageSchema.parse(V1_X_AGENT_UPDATE_FIXTURE);
    expect(parsed.type).toBe("agent_update");
  });
});
```

<!-- RESERVED_FIELDS shape (per research §4.3 verbatim): -->

```typescript
export const RESERVED_FIELDS = {
  // CreateAgentRequestMessage: ["legacyHostId"],   // populated as fields complete sunset
} as const satisfies Record<string, readonly string[]>;
```

<!-- @deprecated annotation pattern (per D-08, applied to chromeEnabled in Plan 03): -->

```typescript
chromeEnabled: z
  .boolean()
  .optional()
  .describe("@deprecated since=v1.11 use=`chromeLayoutEnabled`+`keyboardShortcutsEnabled` removeAfter=v1.16"),
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RESERVED_FIELDS registry + ARCH-02 discipline header to messages.ts</name>
  <files>packages/server/src/shared/messages.ts</files>
  <read_first>
    - packages/server/src/shared/messages.ts (lines 1-80) — read existing imports/header to find correct insertion point above first schema export
    - .planning/research/ARCHITECTURE.md §4 (verbatim Stripe + Protobuf hybrid pattern, `RESERVED_FIELDS`, `@deprecated since= removeAfter=`)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"`packages/server/src/shared/messages.ts` (MODIFIED)" — exact RESERVED_FIELDS shape and discipline-header text
    - CLAUDE.md "WebSocket / Message Schema Rules" — never narrow, never remove, never optional → required
  </read_first>
  <behavior>
    - Importing `RESERVED_FIELDS` from `./messages.js` returns a `Record<string, readonly string[]>` value (initially empty `{}` is fine — the registry is populated as fields complete their sunset window).
    - The discipline-header comment block above `RESERVED_FIELDS` documents (a) the `@deprecated since= removeAfter=` convention, (b) the ≥6-minor-release horizon rule, (c) the "once removed, name is reserved forever" rule.
    - File still parses (`tsc` clean) and existing tests still pass.
  </behavior>
  <action>
    1. Open `packages/server/src/shared/messages.ts`. Find the first schema `export const ...Schema = z.object(...)` declaration after the imports block.
    2. Immediately ABOVE that first schema export, insert the discipline-header comment block (verbatim text from 01-PATTERNS.md §"`packages/server/src/shared/messages.ts` (MODIFIED)") followed by:

    ```typescript
    /**
     * Field names removed from the schema and reserved forever (per ARCH-02 +
     * Protobuf-style "reserved" discipline). Adding a name here means: the wire
     * key is dead, but no future field may reuse the name with different meaning.
     */
    export const RESERVED_FIELDS = {
      // Example shape — populated as fields complete their sunset window.
      // CreateAgentRequestMessage: ["legacyHostId"],
    } as const satisfies Record<string, readonly string[]>;
    ```

    3. Do NOT modify any existing schema declarations in this task. Annotation of `chromeEnabled` (D-08) is owned by Plan 03 because the flag-store split is Plan 03's surface.
    4. Run `npm run typecheck` to confirm `tsc` clean.

  </action>
  <verify>
    <automated>grep -q "export const RESERVED_FIELDS" packages/server/src/shared/messages.ts && grep -q "@deprecated since=" packages/server/src/shared/messages.ts && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const RESERVED_FIELDS = {" packages/server/src/shared/messages.ts` returns `1`
    - `grep -c "satisfies Record<string, readonly string\[\]>" packages/server/src/shared/messages.ts` returns at least `1`
    - `grep -c "ARCH-02" packages/server/src/shared/messages.ts` returns at least `1`
    - `npm run typecheck` exits 0
    - `git diff packages/server/src/shared/messages.ts | grep -c '^-' | awk '{print ($1 < 5)}'` returns `1` (only additions, no schema fields removed/narrowed — backward-compat hard rule)
  </acceptance_criteria>
  <done>RESERVED_FIELDS exported with the documented header; typecheck clean; no existing schema field touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land frozen-fixture parse tests for v1.8, v1.9, v1.10</name>
  <files>
    packages/server/src/shared/messages.frozen-v1.8.test.ts,
    packages/server/src/shared/messages.frozen-v1.9.test.ts,
    packages/server/src/shared/messages.frozen-v1.10.test.ts
  </files>
  <read_first>
    - packages/server/src/shared/messages.attachments.test.ts (lines 1-80) — exact analog for vitest + Zod parse pattern
    - packages/server/src/shared/messages.ts (skim discriminated union exports, especially `WSInboundMessageSchema`, `AgentUpdateMessageSchema`, `CreateAgentRequestMessageSchema`, `SendAgentMessageRequestSchema`)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"`packages/server/src/shared/messages.frozen-v1.{8,9,10}.test.ts`" — file layout, three assertion classes per fixture
    - .planning/research/PITFALLS.md pitfall 3 ("behavioral compatibility, not just type signature")
  </read_first>
  <behavior>
    For EACH of the three files (v1.8, v1.9, v1.10):
    - Test 1: today's `AgentUpdateMessageSchema.parse(V1_X_AGENT_UPDATE_FIXTURE)` succeeds and `parsed.type === "agent_update"` (forward-compat: a v1.X-shaped daemon-emitted message parses today).
    - Test 2: today's `CreateAgentRequestMessageSchema.parse(V1_X_CREATE_AGENT_FIXTURE)` succeeds and `parsed.type === "create_agent_request"` (back-compat: a v1.X-shaped client request parses today).
    - Test 3: today's `SendAgentMessageRequestSchema.parse(V1_X_SEND_MESSAGE_FIXTURE)` succeeds and a key derivation a v1.X client makes (e.g. `parsed.text || ""`, or `parsed.attachments?.length ?? 0`) returns the same value class.

    Frozen fixtures must include AT LEAST: `agent_update` outbound, `create_agent_request` inbound, `send_agent_message_request` inbound, `agent_permission_response` inbound (so the carve in Plan 04 has coverage on the permission path).

    Each fixture is declared `as const` and is annotated with `// FROZEN — do not edit.` to make accidental edits stand out in PR review.

  </behavior>
  <action>
    1. Read `packages/server/src/shared/messages.attachments.test.ts` lines 1-80 in full — use this as the structural template (imports, `describe`, `it`, `.parse()` shape).
    2. For v1.10 (most recent shipped — least drift, easiest to capture as today's schema MINUS any v1.11 additions): create `packages/server/src/shared/messages.frozen-v1.10.test.ts`. The fixture for `AgentUpdateMessageSchema` is captured by literally calling `.parse({...})` against a hand-rolled minimal payload that includes ONLY fields known to be in the v1.10 wire (planner uses `git log --oneline -- packages/server/src/shared/messages.ts | head -50` to identify which fields were already present at v1.10; if uncertain, default to "minimal payload with required fields only" — this still proves back-compat for the most-conservative v1.10 client).
    3. For v1.9 and v1.8: same pattern, but progressively SMALLER fixtures (older clients knew about fewer fields). Each file's fixture set MUST be independent — do not share fixtures across versions.
    4. Each test file MUST exercise these four schemas at minimum: `AgentUpdateMessageSchema`, `CreateAgentRequestMessageSchema`, `SendAgentMessageRequestSchema`, `WSInboundMessageSchema` (parse a `{type: "agent_permission_response", ...}` fragment through the discriminated union).
    5. Add the marker comment `// FROZEN — do not edit. Snapshot of v1.X-shipped wire shapes.` immediately above each fixture constant.
    6. Annotate each fixture object as `as const` so any accidental schema-narrowing on the daemon side surfaces as a TS error first, runtime parse error second.
    7. Run only the three new files via vitest (CLAUDE.md rule: never run the full suite). Each must exit 0.

    Hand-rolling fixtures rather than capturing real wire traffic is acceptable here because (a) we don't have a v1.8 daemon running to snapshot, (b) the schema source IS the contract, and (c) the test asserts intersection — "what v1.X knew about" — which is conservative by construction. If a future field is later mistakenly made required, this test fails because the v1.X fixture omits it.

  </action>
  <verify>
    <automated>npx vitest run packages/server/src/shared/messages.frozen-v1.8.test.ts packages/server/src/shared/messages.frozen-v1.9.test.ts packages/server/src/shared/messages.frozen-v1.10.test.ts --bail=1 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/server/src/shared/messages.frozen-v1.8.test.ts` exits 0
    - `test -f packages/server/src/shared/messages.frozen-v1.9.test.ts` exits 0
    - `test -f packages/server/src/shared/messages.frozen-v1.10.test.ts` exits 0
    - `grep -l 'describe("v1.8 wire compatibility"' packages/server/src/shared/messages.frozen-v1.8.test.ts` matches
    - `grep -l 'describe("v1.9 wire compatibility"' packages/server/src/shared/messages.frozen-v1.9.test.ts` matches
    - `grep -l 'describe("v1.10 wire compatibility"' packages/server/src/shared/messages.frozen-v1.10.test.ts` matches
    - Each test file contains at least 4 `it(` blocks: `grep -c '^\s*it(' packages/server/src/shared/messages.frozen-v1.8.test.ts` returns `>= 4` (and same for v1.9, v1.10)
    - Each fixture is marked frozen: `grep -c "FROZEN — do not edit" packages/server/src/shared/messages.frozen-v1.10.test.ts` returns `>= 3` (one per fixture constant)
    - `npx vitest run packages/server/src/shared/messages.frozen-v1.8.test.ts packages/server/src/shared/messages.frozen-v1.9.test.ts packages/server/src/shared/messages.frozen-v1.10.test.ts --bail=1` exits 0
    - All three files import from `./messages.js`: `grep -c "from \"./messages.js\"" packages/server/src/shared/messages.frozen-v1.{8,9,10}.test.ts | awk -F: '{s+=$2} END {print (s == 3)}'` returns `1`
  </acceptance_criteria>
  <done>Three frozen-fixture test files exist, each green, each pins v1.X wire compat for at least four schemas including the permission path Plan 04 will carve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add warn-only @deprecated-annotation lint script + npm wiring</name>
  <files>
    tools/lint/deprecated-annotation.ts,
    tools/lint/deprecated-annotation.test.ts,
    tools/lint/README.md,
    package.json
  </files>
  <read_first>
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"No Analog Found" — explicit "no in-repo precedent for custom oxlint rules; planner picks oxlint plugin vs custom AST script"
    - .planning/research/ARCHITECTURE.md §4.3 step 5 — every `@deprecated` in `messages.ts` `.describe()` without both `since=` and `removeAfter=` warns
    - CLAUDE.md "Always use npm scripts for linting and formatting" — must wire as `npm run lint:schema` (or named target inside package.json scripts), never invoked via `npx` directly by humans
    - package.json scripts section — see existing `lint`, `typecheck`, `format` script naming for parity
  </read_first>
  <behavior>
    - `npm run lint:schema` runs the new script and exits 0 today (warn-only — there are zero `@deprecated` annotations yet, so zero warnings).
    - When Plan 03 adds `chromeEnabled` with `@deprecated since=v1.11 use=... removeAfter=v1.16`, the lint accepts that line as VALID (both markers present).
    - When a hypothetical contributor writes `@deprecated since=v1.11` (missing `removeAfter=`), the lint emits a warning to stderr referencing the file:line and exits 0 (warn-only Phase 1; promoted to error in Phase 5).
    - The lint scans only `packages/server/src/shared/messages.ts` (Phase 1 scope; broadened in Phase 5 along with promotion).
    - Self-test (`tools/lint/deprecated-annotation.test.ts`) exercises three cases: (a) compliant annotation passes, (b) missing `removeAfter=` warns, (c) missing `since=` warns.
  </behavior>
  <action>
    1. Create `tools/lint/deprecated-annotation.ts` — Node TypeScript module (run via `tsx`) exporting `function lintDeprecatedAnnotations(filePath: string): { violations: Array<{ line: number; message: string }> }`. Implementation strategy: read file as text, scan for `.describe("` strings, capture the substring up to closing `")`, regex-test for `@deprecated`. If found, require both `/since=v\d+\.\d+/` AND `/removeAfter=v\d+\.\d+/` substrings; emit a violation if either is missing. No AST parser needed (kept simple per CONVENTIONS.md "zero complexity budget" and the warn-only scope).
    2. Add a `main()` entry that, when the file is run directly (`tsx tools/lint/deprecated-annotation.ts`), defaults to scanning `packages/server/src/shared/messages.ts`, prints any violations as `WARN  {file}:{line}  {message}` to stderr, and ALWAYS exits 0 (warn-only this phase). Promoted to error in Phase 5 by flipping the exit code; leave a `// PHASE 5: change exit code to 1 when promoting from warn to error per ROADMAP P5 success criterion #2` comment at the exit line.
    3. Create `tools/lint/deprecated-annotation.test.ts` (vitest) covering the three cases from `<behavior>`. Use a temporary file written via `node:fs` `mkdtempSync` + `writeFileSync` rather than fixture files in repo (avoids polluting the repo with intentionally-broken fixtures).
    4. Create `tools/lint/README.md` (≤30 lines) describing: rule purpose, current scope (`messages.ts` only), warn-level enforcement (Phase 1) → error promotion (Phase 5 per ROADMAP), how to add scope, how to run (`npm run lint:schema`).
    5. Add to `package.json` root `scripts`:
       - `"lint:schema": "tsx tools/lint/deprecated-annotation.ts"`
       Insert next to existing `"lint"` for visual grouping. Do NOT modify the existing `"lint"` target — that wraps oxlint and is a separate concern (per CLAUDE.md "always use npm scripts").
    6. Verify `npm run lint:schema` exits 0 against today's `messages.ts` (no `@deprecated` annotations exist yet — zero warnings).
    7. Verify `npx vitest run tools/lint/deprecated-annotation.test.ts --bail=1` exits 0.
    8. Run `npm run typecheck` and `npm run lint` to confirm no regressions in repo-wide checks.
  </action>
  <verify>
    <automated>npm run lint:schema 2>&1 | tail -5 && npx vitest run tools/lint/deprecated-annotation.test.ts --bail=1 2>&1 | tail -10 && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/lint/deprecated-annotation.ts` exits 0
    - `test -f tools/lint/deprecated-annotation.test.ts` exits 0
    - `test -f tools/lint/README.md` exits 0
    - `grep -c "lint:schema" package.json` returns at least `1`
    - `grep -c "tsx tools/lint/deprecated-annotation.ts" package.json` returns at least `1`
    - `grep -c "PHASE 5: change exit code" tools/lint/deprecated-annotation.ts` returns at least `1` (cleanup marker present for Plan-5-future-self)
    - `grep -c "lintDeprecatedAnnotations" tools/lint/deprecated-annotation.ts` returns at least `2` (export + at least one in-file consumer)
    - `npm run lint:schema` exits 0 (warn-only, zero violations against current `messages.ts`)
    - `npx vitest run tools/lint/deprecated-annotation.test.ts --bail=1` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Schema-discipline lint script exists, npm-wired, self-tested, exits 0 today; ready for Plan 03 to add the first real `@deprecated` annotation against it.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                         |
| ---------------------------------------- | ------------------------------------------------------------------- |
| Old client (v1.8/v1.9/v1.10) → daemon WS | Untrusted-shape input crosses into daemon Zod parse                 |
| New daemon → old client                  | Outbound message shape MUST be a superset old client schemas accept |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------- | -------------------------- | ---------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-01   | T (Tampering)              | `messages.ts` schemas — risk of accidental narrowing (e.g. optional → required, string → enum) | mitigate    | Frozen-fixture tests in Task 2 fail CI on any narrowing — they parse v1.X-minimal fixtures, so removing optionality breaks the parse.                                                 |
| T-01-02   | I (Information disclosure) | Frozen fixtures might inadvertently snapshot real session content / tokens / secrets           | mitigate    | Task 2 instructions explicitly hand-roll fixtures (do NOT capture from running daemon); fixtures contain only structural placeholders. Reviewer-verifiable via reading the constants. |
| T-01-03   | D (DoS)                    | Lint script could OOM or hang on large schema files                                            | accept      | Script is single-file scan of `messages.ts` (~3741 lines, well under any concern); warn-only this phase, run on-demand not on every keystroke. Risk is negligible.                    |
| T-01-04   | R (Repudiation)            | A schema-narrowing PR could land without traceability                                          | mitigate    | `RESERVED_FIELDS` registry + `@deprecated since= removeAfter=` annotations create an in-repo audit trail; lint surfaces violations in CI logs.                                        |

**Note:** This plan ships zero new attack surface. It strengthens the schema-evolution discipline that protects the WebSocket boundary (the most critical untrusted-input crossing in Ottie). All threats listed are about preventing future regressions, not new exposures.
</threat_model>

<verification>
1. `npm run typecheck` exits 0 (no schema field accidentally narrowed).
2. `npm run lint:schema` exits 0 (warn-only baseline established).
3. `npx vitest run packages/server/src/shared/messages.frozen-v1.8.test.ts packages/server/src/shared/messages.frozen-v1.9.test.ts packages/server/src/shared/messages.frozen-v1.10.test.ts --bail=1` exits 0.
4. `npx vitest run tools/lint/deprecated-annotation.test.ts --bail=1` exits 0.
5. `git diff packages/server/src/shared/messages.ts` shows ONLY additions (RESERVED_FIELDS + comment block) and no removals or narrowing of existing fields.
6. The repo-wide `npm run lint` and `npm run format:check` both exit 0.
</verification>

<success_criteria>

- `RESERVED_FIELDS` exported from `packages/server/src/shared/messages.ts`
- Three `messages.frozen-v1.{8,9,10}.test.ts` files exist and pass vitest
- `npm run lint:schema` script wired into `package.json` and exits 0
- `tools/lint/deprecated-annotation.ts` self-tested via `tools/lint/deprecated-annotation.test.ts`
- All four checks green: typecheck, lint, lint:schema, format:check
- ARCH-02 foundation laid; Plan 04 (carve) can now safely commit against the CI guardrail
  </success_criteria>

<output>
After completion, create `.planning/phases/01-architectural-foundations-gating-bug-fixes/01-01-SUMMARY.md` capturing:
- Files created / modified
- Test counts (number of `it(...)` blocks per frozen-vX.Y file)
- The exact `npm run lint:schema` baseline output (should be empty)
- Any deviations from the plan and why
- Pointers Plan 04 (carve) needs: which schemas the frozen fixtures exercise, where to add fixtures if a new schema kind ships
</output>
