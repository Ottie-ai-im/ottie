---
phase: 01-architectural-foundations-gating-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/styles/tokens/primitives.ts
  - packages/app/src/styles/tokens/semantic.light.ts
  - packages/app/src/styles/tokens/semantic.dark.ts
  - packages/app/src/styles/tokens/component.ts
  - packages/app/src/styles/tokens/motion.ts
  - packages/app/src/styles/tokens/typography.ts
  - packages/app/src/styles/theme.ts
  - packages/app/src/components/ui/glass-surface.tsx
  - packages/app/src/components/daemon-connection-dot.tsx
  - packages/app/src/components/daemon-connection-dot.test.tsx
  - packages/app/src/components/math-curve-loader/curves.ts
  - tools/lint/hardcoded-color.ts
  - tools/lint/hardcoded-color.test.ts
  - tools/lint/hardcoded-color.baseline.json
  - package.json
autonomous: true
requirements:
  - THM-01
tags:
  - theme
  - tokens
  - unistyles
  - lint
  - design-system
must_haves:
  truths:
    - "A developer can import palette/spacing/radius from @/styles/tokens/primitives and consume them via the new semantic-token tree (no inline hex/rgb in migrated components)"
    - "Light and dark themes render identically before and after the migration (visual parity, structural change only)"
    - "glass-surface.tsx, daemon-connection-dot.tsx, math-curve-loader/curves.ts no longer reach through to theme.colors.palette.* or hard-coded color literals"
    - "A warn-only lint flags new hardcoded #xxx / rgb() / rgba() color literals under packages/app/src/ outside tokens/ and palette declarations"
    - "A counter-test guarantees the warn count never increases (CI baseline written to tools/lint/hardcoded-color.baseline.json)"
  artifacts:
    - path: "packages/app/src/styles/tokens/primitives.ts"
      provides: "palette + spacing + radius primitive tokens"
      exports: ["palette", "spacing", "radius"]
    - path: "packages/app/src/styles/tokens/semantic.light.ts"
      provides: "Light-mode semantic tokens (surface, text, status nested namespaces)"
      exports: ["semanticLight"]
    - path: "packages/app/src/styles/tokens/semantic.dark.ts"
      provides: "Dark-mode semantic tokens (mirrors semanticLight shape)"
      exports: ["semanticDark"]
    - path: "packages/app/src/styles/tokens/component.ts"
      provides: "Component-level tokens (radii, border widths)"
      exports: ["componentTokens"]
    - path: "packages/app/src/styles/tokens/motion.ts"
      provides: "Motion curves + durations + math-curve timing"
      exports: ["motion"]
    - path: "packages/app/src/styles/tokens/typography.ts"
      provides: "Typography tokens (fontFamily, fontSize, lineHeight, weight)"
      exports: ["typography"]
    - path: "packages/app/src/styles/theme.ts"
      provides: "Composition root spreading tokens into lightTheme / darkTheme"
      contains: 'from "./tokens/primitives'
    - path: "tools/lint/hardcoded-color.ts"
      provides: "Warn-level lint script for hardcoded color literals"
      exports: ["lintHardcodedColors"]
    - path: "tools/lint/hardcoded-color.baseline.json"
      provides: "CI counter-baseline (per D-10 + THM-01 counter-test)"
  key_links:
    - from: "packages/app/src/styles/theme.ts"
      to: "packages/app/src/styles/tokens/{primitives,semantic.light,semantic.dark,component,motion,typography}.ts"
      via: "import statements from each tokens file"
      pattern: 'from "./tokens/'
    - from: "packages/app/src/components/ui/glass-surface.tsx"
      to: "theme.surface.glass.{tint,tintStrong,border}"
      via: "Unistyles theme accessor"
      pattern: "theme.surface.glass"
    - from: "packages/app/src/components/daemon-connection-dot.tsx"
      to: "theme.status.{online,connecting,offline}"
      via: "Unistyles theme accessor"
      pattern: "theme.status"
    - from: "package.json scripts.lint:colors"
      to: "tools/lint/hardcoded-color.ts"
      via: "node script invocation"
      pattern: "tools/lint/hardcoded-color"
---

<objective>
Land the v1.11 theme-token skeleton (per D-09 / D-11 / D-12) on top of Unistyles, lift the in-flight color/motion/typography work into the new three-tier structure (primitive → semantic → component), and migrate the four in-flight files (theme.ts, glass-surface.tsx, daemon-connection-dot.tsx, math-curve-loader/curves.ts) to consume semantic tokens instead of reaching through to primitives or inline literals.

Purpose (per ROADMAP P1 success criterion #2 + research §8.3): every Phase 4 surface migration depends on this skeleton existing and being self-consistent. Phase 1 ships the structure plus the targeted migration of the four files already touching colors/surfaces/motion. All other surface migrations are explicitly Phase 4 (per D-09 deferred list).

Constraints honored:

- Phase 1 is structural, not visual (PITFALLS pitfall 1) — every existing color value must round-trip identically.
- TS only, no DTCG JSON pipeline (per D-12).
- Hardcoded-color lint at warn-level on new files (per D-10), counter-test guarantees the warn count never increases.
- Out of scope: every other surface migration (modals, sheets, popovers, callout cards, toasts).
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-CONTEXT.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>

Token tree shape (per research §8.3 + 01-PATTERNS.md, mirrored exactly):

- `tokens/primitives.ts` exports `palette` (lifted verbatim from theme.ts:1-99 baseColors block, renamed), plus `spacing` and `radius` constants.
- `tokens/semantic.light.ts` imports `palette` and exports `semanticLight` with nested namespaces (`surface`, `text`, `status`, etc.). Glass tints stay as inline rgba (no palette equivalent).
- `tokens/semantic.dark.ts` mirrors `semanticLight`'s shape with dark values.
- `tokens/component.ts` imports `radius` and exports `componentTokens` (glassCard, glassSheet, glassPill, button — with their radius + borderWidth).
- `tokens/motion.ts` exports `motion` with `curves` (cubic-bezier strings), `durations` (fast/normal/slow ms), and `mathCurves.<curveName>` (per-preset durationMs / rotationDurationMs / pulseDurationMs lifted from curves.ts:173-262).
- `tokens/typography.ts` exports `typography` with `fontFamily`, `fontSize`, `lineHeight`, `weight`.
- `theme.ts` becomes a composition root: spreads `sharedTokens = {palette, spacing, radius, components: componentTokens, motion, typography}` plus the semantic tree into `lightTheme` and `darkTheme`. Existing alternate dark variants (darkZinc, darkMidnight, darkClaude, darkGhostty) keep their override pattern but spread `semanticDark` instead of `darkSemanticColors`.

Backward-compat shim (CRITICAL): non-migrated consumers still consume `theme.colors.surfaceGlass` / `theme.borderRadius.glassPill` etc. The composition root also spreads the LEGACY `lightSemanticColors` literal under a `colors:` key on each theme. Comment this `// LEGACY shim — removed surface-by-surface in Phase 4 (THM-02..04).`

Consumer rewrites:

`glass-surface.tsx:47-64` BEFORE: `theme.colors.surfaceGlassStrong`, `theme.colors.surfaceGlass`, `theme.colors.borderGlass`. AFTER: `theme.surface.glass.tintStrong`, `theme.surface.glass.tint`, `theme.surface.glass.border`. Same change for `theme.borderRadius.glassPill` → `theme.components.glassPill.radius`.

`daemon-connection-dot.tsx:42-50, 89-91` BEFORE: `theme.colors.palette.green[400]`, `theme.colors.palette.amber[500]`, `theme.colors.palette.red[500]`, `theme.colors.foregroundMuted`. AFTER: `theme.status.online`, `theme.status.connecting`, `theme.status.offline`, `theme.text.muted`. Add `online`/`connecting`/`offline` keys under `semantic.{light,dark}.ts` `status:` namespace, referencing `palette.green[400]`/`palette.amber[500]`/`palette.red[500]`.

`math-curve-loader/curves.ts` BEFORE: each preset hard-codes `durationMs`, `rotationDurationMs`, `pulseDurationMs`. AFTER: `import { motion } from "@/styles/tokens/motion";` then each preset reads timing from `motion.mathCurves.<curveName>`. Parametric `point` functions stay in this file.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the tokens tree (primitives, semantic.light, semantic.dark, component, motion, typography) and rewire theme.ts as composition root</name>
  <files>
    packages/app/src/styles/tokens/primitives.ts,
    packages/app/src/styles/tokens/semantic.light.ts,
    packages/app/src/styles/tokens/semantic.dark.ts,
    packages/app/src/styles/tokens/component.ts,
    packages/app/src/styles/tokens/motion.ts,
    packages/app/src/styles/tokens/typography.ts,
    packages/app/src/styles/theme.ts
  </files>
  <read_first>
    - packages/app/src/styles/theme.ts (full file, 716 lines — source of truth for every existing value that must round-trip into the new tree)
    - packages/app/src/styles/unistyles.ts (the wiring that consumes lightTheme/darkTheme — must NOT change shape per D-11)
    - packages/app/src/components/math-curve-loader/curves.ts (lines 173-262 — CURVE_PRESETS block; timing values move to motion.ts, parametric `point` functions stay)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md sections "tokens/primitives.ts" through "theme.ts (MODIFIED)" — verbatim analog tables and import patterns
    - .planning/research/ARCHITECTURE.md §8 (three-tier token layering, Unistyles 3 wiring rationale)
    - .planning/research/PITFALLS.md pitfall 5 (Polaris-v11 lesson: alias tokens reference primitives, components NEVER consume primitives directly)
    - CLAUDE.md "File organization" — no barrel index.ts (the new tokens/ directory MUST NOT have an index.ts)
  </read_first>
  <behavior>
    - All six token files exist under packages/app/src/styles/tokens/. None re-exports from a sibling — consumers import the specific tree path.
    - palette in primitives.ts is byte-for-byte equivalent to today's baseColors in theme.ts:1-99 (rename baseColors -> palette, lift verbatim).
    - semanticLight and semanticDark reference palette.* for any color that appears in palette (per PITFALLS pitfall 5). Glass tints stay as inline rgba (no palette equivalent).
    - theme.ts becomes a thin composition root per <interfaces>. Every existing exported theme object name (lightTheme, darkTheme, darkZinc, darkMidnight, darkClaude, darkGhostty) is preserved.
    - unistyles.ts is NOT modified (per D-11).
    - Backward-compat shim present: `colors:` key on each theme exposing the flat legacy semantic shape so non-migrated consumers (Phase 4 scope) keep working.
  </behavior>
  <action>
    1. tokens/primitives.ts: read theme.ts:1-99 in full. Lift the entire baseColors const block, rename to palette, copy verbatim. Add spacing and radius exports — values come from inspecting theme.ts (search for spacing: and borderRadius: keys; lift verbatim). Use `as const` on every export.
    2. tokens/semantic.light.ts: read theme.ts:132-219 in full (lightSemanticColors). Reorganize into nested namespaces (surface, text, status, border, etc.) per the <interfaces> guidance. EVERY existing key in lightSemanticColors MUST appear under exactly one new namespace path — write a comment block at the top mapping `OLD_KEY -> new.namespace.key` so future surface migrations have a key map. Reference palette.* instead of inline hex wherever a palette match exists; preserve inline rgba for glass tints.
    3. tokens/semantic.dark.ts: same exercise on the dark mirror. Structurally identical to semanticLight (same key paths, different values).
    4. tokens/component.ts: lift borderRadius keys (glassCard, glassSheet, glassPill, button) into componentTokens. Reference radius.* from primitives where matched; otherwise inline px with a `// inline: no primitive match` comment.
    5. tokens/motion.ts: read curves.ts:173-262. Extract ONLY the timing keys (durationMs, rotationDurationMs, pulseDurationMs) per math-curve into motion.mathCurves.<curveName>. Add motion.curves (cubic-bezier strings) and motion.durations ({fast: 120, normal: 200, slow: 320}) per research §8.6. Parametric point functions stay in curves.ts.
    6. tokens/typography.ts: lift theme.ts fontFamily, fontSize, lineHeight, weight (or fontWeight) fragments verbatim.
    7. theme.ts rewrite: replace contents with the composition-root shape from <interfaces>. Preserve every existing exported theme object name. Each alternate dark variant continues to override a small set of keys against semanticDark.
    8. Backward-compat shim: each theme also exposes a `colors:` top-level key whose value is the FLAT legacy semantic shape (the original lightSemanticColors / darkSemanticColors literal — copy from theme.ts BEFORE deletion). Add comment `// LEGACY shim — removed surface-by-surface in Phase 4 (THM-02..04). See ROADMAP §"Phase 4".`
    9. Run `npm run typecheck`. TS must be clean.
    10. Run `npm run lint -- packages/app/src/styles/`. Must be clean.
  </action>
  <verify>
    <automated>test -f packages/app/src/styles/tokens/primitives.ts && test -f packages/app/src/styles/tokens/semantic.light.ts && test -f packages/app/src/styles/tokens/semantic.dark.ts && test -f packages/app/src/styles/tokens/component.ts && test -f packages/app/src/styles/tokens/motion.ts && test -f packages/app/src/styles/tokens/typography.ts && ! test -f packages/app/src/styles/tokens/index.ts && grep -q tokens/primitives packages/app/src/styles/theme.ts && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All six token files exist (per `test -f` checks above)
    - `! test -f packages/app/src/styles/tokens/index.ts` (no barrel — per CONVENTIONS.md)
    - `grep -c "export const palette" packages/app/src/styles/tokens/primitives.ts` returns `1`
    - `grep -c "export const semanticLight" packages/app/src/styles/tokens/semantic.light.ts` returns `1`
    - `grep -c "export const semanticDark" packages/app/src/styles/tokens/semantic.dark.ts` returns `1`
    - `grep -c "export const componentTokens" packages/app/src/styles/tokens/component.ts` returns `1`
    - `grep -c "export const motion" packages/app/src/styles/tokens/motion.ts` returns `1`
    - `grep -c "export const typography" packages/app/src/styles/tokens/typography.ts` returns `1`
    - `grep -c "tokens/primitives" packages/app/src/styles/theme.ts` returns at least `1` (composition-root pattern)
    - `grep -c "export const lightTheme" packages/app/src/styles/theme.ts` returns `1` (existing public symbol preserved)
    - `grep -c "export const darkTheme" packages/app/src/styles/theme.ts` returns `1` (existing public symbol preserved)
    - `grep -c "LEGACY shim" packages/app/src/styles/theme.ts` returns at least `1` (Phase-4 cleanup marker)
    - `npm run typecheck` exits 0
    - `npm run lint -- packages/app/src/styles/` exits 0
    - `wc -l packages/app/src/styles/theme.ts | awk '{print ($1 < 716)}'` returns `1` (theme.ts shrunk — values lifted into tokens/)
  </acceptance_criteria>
  <done>Six-file token tree exists with no barrel; theme.ts is composition-root only; legacy `colors:` shim preserves non-migrated consumers; typecheck + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the four in-flight consumers (glass-surface, daemon-connection-dot, math-curve-loader/curves) to semantic tokens</name>
  <files>
    packages/app/src/components/ui/glass-surface.tsx,
    packages/app/src/components/daemon-connection-dot.tsx,
    packages/app/src/components/daemon-connection-dot.test.tsx,
    packages/app/src/components/math-curve-loader/curves.ts
  </files>
  <read_first>
    - packages/app/src/components/ui/glass-surface.tsx (full file) — current theme.colors.surfaceGlass* consumption at lines 47-64 + theme.borderRadius.glassPill at lines 39-45
    - packages/app/src/components/daemon-connection-dot.tsx (full file) — theme.colors.palette.* reach-through at lines 42-50, 79-93 + theme.colors.foregroundMuted at line 91
    - packages/app/src/components/math-curve-loader/curves.ts (full file) — CURVE_PRESETS block at 173-262 + parametric point functions
    - packages/app/src/styles/tokens/{semantic.light,semantic.dark,component,motion}.ts (created in Task 1) — destination namespaces
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md sections "glass-surface.tsx (MODIFIED)", "daemon-connection-dot.tsx (MODIFIED)", "tokens/motion.ts (NEW) — lift from math-curve-loader" — exact before/after rewrites
  </read_first>
  <behavior>
    - glass-surface.tsx reads theme.surface.glass.tint, theme.surface.glass.tintStrong, theme.surface.glass.border, and theme.components.glassCard.radius (etc.) instead of theme.colors.surfaceGlass / theme.colors.borderGlass / theme.borderRadius.glassPill.
    - daemon-connection-dot.tsx reads theme.status.online, theme.status.connecting, theme.status.offline, and theme.text.muted instead of theme.colors.palette.green[400] / theme.colors.foregroundMuted.
    - math-curve-loader/curves.ts imports motion.mathCurves.<curveName> from @/styles/tokens/motion and constructs CURVE_PRESETS by combining the imported timing values with the locally-defined parametric point functions. Net file shrinks (timing values moved out).
    - Existing visual output is byte-for-byte identical (Phase 1 is structural). The new daemon-connection-dot.test.tsx verifies the three status branches resolve to the same palette values via the semantic tokens.
    - Zero theme.colors.palette.* references remain in any of the three migrated files (grep-checked).
  </behavior>
  <action>
    1. glass-surface.tsx: apply the rewrite from <interfaces>. Update both the useMemo dependency array (drop theme.colors.surfaceGlass, add theme.surface.glass) and the styled body. Same exercise for theme.borderRadius.glassPill etc. → theme.components.glassPill.radius.
    2. daemon-connection-dot.tsx: apply the rewrite from <interfaces>. The status if/else cascade at lines 42-50 → theme.status.online etc. The text reference at line 89-91 (theme.colors.foregroundMuted) → theme.text.muted.
    3. math-curve-loader/curves.ts: add `import { motion } from "@/styles/tokens/motion";` at the top. Replace each preset's hard-coded durationMs/rotationDurationMs/pulseDurationMs with motion.mathCurves.<curveName>.* . Keep `point: lemniscatePoint` and the math functions verbatim. The name/rotate/particleCount/trailSpan/strokeWidth keys stay in this file.
    4. Add packages/app/src/components/daemon-connection-dot.test.tsx — minimal vitest test that imports daemon-connection-dot.tsx, renders with each status (`online` | `connecting` | `offline`), and asserts the rendered dot color attribute equals the corresponding theme.status.* value drawn from semanticLight. This is the visual-parity guard.
    5. Run `npm run typecheck`.
    6. Run `npm run lint -- packages/app/src/components/ui/glass-surface.tsx packages/app/src/components/daemon-connection-dot.tsx packages/app/src/components/math-curve-loader/curves.ts`.
    7. Run only the new test (CLAUDE.md): `npx vitest run packages/app/src/components/daemon-connection-dot.test.tsx --bail=1`.
    8. Run `npm run format -- packages/app/src/components/ui/glass-surface.tsx packages/app/src/components/daemon-connection-dot.tsx packages/app/src/components/math-curve-loader/curves.ts`.
  </action>
  <verify>
    <automated>! grep -q "theme.colors.palette" packages/app/src/components/daemon-connection-dot.tsx && ! grep -q "theme.colors.palette" packages/app/src/components/ui/glass-surface.tsx && grep -q "theme.surface.glass" packages/app/src/components/ui/glass-surface.tsx && grep -q "theme.status" packages/app/src/components/daemon-connection-dot.tsx && grep -q "tokens/motion" packages/app/src/components/math-curve-loader/curves.ts && npx vitest run packages/app/src/components/daemon-connection-dot.test.tsx --bail=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "theme.colors.palette" packages/app/src/components/daemon-connection-dot.tsx` returns `0`
    - `grep -c "theme.colors.palette" packages/app/src/components/ui/glass-surface.tsx` returns `0`
    - `grep -c "theme.colors.palette" packages/app/src/components/math-curve-loader/curves.ts` returns `0`
    - `grep -c "theme.surface.glass" packages/app/src/components/ui/glass-surface.tsx` returns at least `2` (tint + border references)
    - `grep -c "theme.status" packages/app/src/components/daemon-connection-dot.tsx` returns at least `3` (online + connecting + offline)
    - `grep -c "theme.text.muted" packages/app/src/components/daemon-connection-dot.tsx` returns at least `1`
    - `grep -c "tokens/motion" packages/app/src/components/math-curve-loader/curves.ts` returns at least `1`
    - `test -f packages/app/src/components/daemon-connection-dot.test.tsx` exits 0
    - `npx vitest run packages/app/src/components/daemon-connection-dot.test.tsx --bail=1` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint -- packages/app/src/components/ui/glass-surface.tsx packages/app/src/components/daemon-connection-dot.tsx packages/app/src/components/math-curve-loader/curves.ts` exits 0
    - `wc -l packages/app/src/components/math-curve-loader/curves.ts | awk '{print ($1 < 263)}'` returns `1` (file shrank — timing lifted out)
  </acceptance_criteria>
  <done>Three migrated consumers use semantic tokens; visual parity verified by daemon-connection-dot.test.tsx; zero palette reach-through; typecheck + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add warn-only hardcoded-color lint script + counter-baseline + npm wiring</name>
  <files>
    tools/lint/hardcoded-color.ts,
    tools/lint/hardcoded-color.test.ts,
    tools/lint/hardcoded-color.baseline.json,
    package.json
  </files>
  <read_first>
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"No Analog Found" — explicit "no in-repo precedent for custom oxlint rules; planner picks oxlint plugin vs custom AST script" — choose tsx-based simple regex scan, same shape as Plan 01 deprecated-annotation lint
    - .planning/research/ARCHITECTURE.md §8 + PITFALLS pitfall 5 — what the rule must catch (#xxx, rgb(), rgba()) and what it must allow (anything inside packages/app/src/styles/tokens/ and the `palette` declaration in primitives.ts)
    - REQUIREMENTS.md THM-01 — counter-test: CI guarantees the warn count never increases (current count becomes the baseline; PRs adding new violations fail CI)
    - CLAUDE.md "Always use npm scripts for linting and formatting" — must wire as `npm run lint:colors`, NEVER invoked via npx directly
    - tools/lint/deprecated-annotation.ts (created in Plan 01-01) — mirror its tsx-based shape, exit-code conventions, and the `// PHASE 5: change exit code to 1 when promoting from warn to error` marker
  </read_first>
  <behavior>
    - `npm run lint:colors` scans every `.ts` and `.tsx` file under `packages/app/src/`, EXCLUDING `packages/app/src/styles/tokens/` (that's where colors live by definition) and the `palette` const in any file.
    - For each color literal found (regex: `#[0-9a-fA-F]{3,8}` or `rgba?\(`), emit `WARN  {file}:{line}  hardcoded color literal '{snippet}' — use a semantic token from @/styles/tokens/semantic.{light,dark}` to stderr.
    - The script writes the current violation count to `tools/lint/hardcoded-color.baseline.json` ONLY when invoked with `--write-baseline`. Default invocation reads the baseline and exits 0 if `current_count <= baseline_count`, exits 1 (CI failure) if `current_count > baseline_count`. This is the THM-01 counter-test.
    - Initial baseline is captured by running with `--write-baseline` AFTER Tasks 1 and 2 land (so the four migrated files contribute zero violations to the baseline).
    - Self-test (tools/lint/hardcoded-color.test.ts) covers: (a) tokens/ file with hex literals → no violation (excluded path), (b) component file with hex literal → violation emitted, (c) component file with rgba → violation emitted, (d) component file consuming theme.surface.glass.tint → no violation, (e) baseline-comparison: count == baseline → exit 0, count > baseline → exit 1, count < baseline → exit 0 + suggestion to re-baseline.
  </behavior>
  <action>
    1. Create tools/lint/hardcoded-color.ts — Node TypeScript module run via tsx. Export `function lintHardcodedColors(rootDir: string, opts?: { excludePaths?: string[] }): { violations: Array<{ file: string; line: number; snippet: string }>; count: number }`.
    2. Implementation: walk the directory tree under rootDir using `node:fs` (or `fast-glob` if already in devDeps — check package.json first). For each .ts/.tsx file NOT under `tools/`, NOT under `packages/app/src/styles/tokens/`, scan line-by-line for `#[0-9a-fA-F]{3,8}` or `rgba?\(` matches. Skip lines inside multi-line strings or template literals only if trivial (per CONVENTIONS.md "zero complexity budget" — naive line scan is acceptable for warn-level).
    3. Add `main()` that supports two flags: `--write-baseline` (writes count to tools/lint/hardcoded-color.baseline.json) and the default (reads baseline, compares, exits 0 if current <= baseline else 1). Always print violations to stderr regardless of mode. The exit-code path includes a `// PHASE 5: tighten — exit 1 on ANY violation in non-tokens files (THM-01 promotion to error per ROADMAP)` comment for the future-self cleanup task.
    4. After Task 1 and Task 2 are committed (this lint runs against the post-migration tree), invoke `npx tsx tools/lint/hardcoded-color.ts --write-baseline` to capture the initial baseline. Commit `tools/lint/hardcoded-color.baseline.json` with the resulting count.
    5. Create tools/lint/hardcoded-color.test.ts covering all five behavior cases via temp dirs (`mkdtempSync` + `writeFileSync`) — same pattern as Plan 01-01 Task 3.
    6. Add to package.json root scripts (next to the lint:schema entry from Plan 01-01):
       - `"lint:colors": "tsx tools/lint/hardcoded-color.ts"`
       - `"lint:colors:baseline": "tsx tools/lint/hardcoded-color.ts --write-baseline"`
    7. Verify `npm run lint:colors` exits 0 (against the baseline captured in step 4).
    8. Verify `npx vitest run tools/lint/hardcoded-color.test.ts --bail=1` exits 0.
    9. Run `npm run typecheck` and `npm run lint`.
  </action>
  <verify>
    <automated>npm run lint:colors 2>&1 | tail -5 && npx vitest run tools/lint/hardcoded-color.test.ts --bail=1 2>&1 | tail -10 && test -f tools/lint/hardcoded-color.baseline.json && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/lint/hardcoded-color.ts` exits 0
    - `test -f tools/lint/hardcoded-color.test.ts` exits 0
    - `test -f tools/lint/hardcoded-color.baseline.json` exits 0
    - `grep -c "lint:colors" package.json` returns at least `2` (script + baseline-script)
    - `grep -c "tsx tools/lint/hardcoded-color.ts" package.json` returns at least `2`
    - `grep -c "PHASE 5: tighten" tools/lint/hardcoded-color.ts` returns at least `1` (cleanup marker)
    - `grep -c "lintHardcodedColors" tools/lint/hardcoded-color.ts` returns at least `2` (export + in-file consumer)
    - `npm run lint:colors` exits 0 (current violation count equals baseline — counter-test passes)
    - `npx vitest run tools/lint/hardcoded-color.test.ts --bail=1` exits 0
    - `npm run typecheck` exits 0
    - The baseline JSON contains a numeric `count` field: `node -e "const b=require('./tools/lint/hardcoded-color.baseline.json'); process.exit(typeof b.count === 'number' ? 0 : 1)"` exits 0
  </acceptance_criteria>
  <done>Hardcoded-color lint script exists, npm-wired, self-tested, baseline captured POST-migration so the four touched files contribute zero, CI-ready as the THM-01 counter-test guard.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                            |
| -------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Token tree → consumer components | Internal contract; if a semantic key path changes, every consumer breaks at compile time (TS-narrowed) |
| Lint script → CI pipeline        | The lint script's exit code gates merges (warn-only this phase, will be promoted in Phase 5)           |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                                                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                                  |
| --------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-02-01   | T (Tampering)              | semantic.light.ts / semantic.dark.ts — risk that a value drifts and breaks visual parity silently                   | mitigate    | Task 2 adds daemon-connection-dot.test.tsx asserting the three status branches resolve to specific palette values; any drift in semanticLight.status.\* breaks the test. PITFALLS pitfall 1 ("structural, not visual") is the design-time guard. |
| T-02-02   | I (Information disclosure) | Lint script writes baseline JSON to repo                                                                            | accept      | Baseline JSON contains only an integer count; no source content. No risk.                                                                                                                                                                        |
| T-02-03   | D (DoS)                    | Lint script could be slow on large trees                                                                            | accept      | Single recursive walk over packages/app/src/ (~few hundred files); naive line scan; well under 5 seconds. Run on-demand + CI, not on every keystroke.                                                                                            |
| T-02-04   | E (Elevation)              | New token files under @/styles/tokens/ could be imported from packages/server/ leaking client constants into daemon | mitigate    | Existing CONVENTIONS.md import-alias discipline — server uses @server/, never @/. tsconfig path-aliasing already enforces. Plan adds no new export to the daemon side.                                                                           |

**Note:** This plan ships zero new attack surface. Theme tokens are a refactor of existing constants; lint script is a CI-only script with no runtime path. Visual parity (T-02-01) is the only meaningful risk and is mitigated by the new test.
</threat_model>

<verification>
1. `npm run typecheck` exits 0.
2. `npm run lint` exits 0.
3. `npm run lint:colors` exits 0 (counter-baseline guard).
4. `npx vitest run packages/app/src/components/daemon-connection-dot.test.tsx tools/lint/hardcoded-color.test.ts --bail=1` exits 0.
5. `grep -rE "theme\\.colors\\.palette" packages/app/src/components/ui/glass-surface.tsx packages/app/src/components/daemon-connection-dot.tsx packages/app/src/components/math-curve-loader/curves.ts` returns nothing (zero palette reach-through in migrated files).
6. `wc -l packages/app/src/styles/theme.ts` returns a value strictly less than 716 (theme.ts shrank — values lifted into tokens/).
7. The four legacy keys listed in 01-PATTERNS.md backward-compat section (theme.colors.surfaceGlass, theme.colors.foregroundMuted, theme.borderRadius.glassPill, theme.fontSize.xs) still exist via the LEGACY shim (verified by `grep -c "LEGACY shim" packages/app/src/styles/theme.ts >= 1`); non-migrated consumers continue to work.
</verification>

<success_criteria>

- Six new token files exist under packages/app/src/styles/tokens/ with the documented exports
- theme.ts is the composition root; existing public theme objects (lightTheme, darkTheme, dark variants) preserved; LEGACY shim documented
- glass-surface.tsx, daemon-connection-dot.tsx, math-curve-loader/curves.ts use semantic tokens (no palette reach-through)
- daemon-connection-dot.test.tsx asserts visual parity for the three status branches
- Hardcoded-color lint script wired as `npm run lint:colors` with counter-baseline JSON
- All four checks green: typecheck, lint, lint:colors, the new vitest tests
- THM-01 token foundation laid; Phase 4 surface migrations can begin against a stable skeleton
  </success_criteria>

<output>
After completion, create .planning/phases/01-architectural-foundations-gating-bug-fixes/01-02-SUMMARY.md capturing:
- Files created / modified
- The OLD_KEY -> new.namespace.key map written into semantic.light.ts (so Phase 4 has the migration recipe)
- The captured baseline count from tools/lint/hardcoded-color.baseline.json
- Any deviations from the plan and why
- Pointers Phase 4 will need: which legacy keys are still being read by non-migrated consumers (grep results from `theme.colors.*` across packages/app/src/)
</output>
