---
phase: 01-architectural-foundations-gating-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/components/message.tsx
  - packages/app/src/components/resize-handle.tsx
  - packages/server/src/server/agent/providers/opencode-agent.ts
  - packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts
  - packages/app/src/app/_layout.tsx
  - packages/app/src/i18n/locales/en.json
  - packages/app/src/i18n/locales/zh.json
  - tools/lint/is-hovered-alone.ts
  - tools/lint/is-hovered-alone.test.ts
  - tools/lint/is-hovered-alone.baseline.json
  - tools/lint/pointer-events-web-only.ts
  - tools/lint/pointer-events-web-only.test.ts
  - tools/lint/pointer-events-web-only.baseline.json
  - package.json
autonomous: true
requirements:
  - NAV-A3
  - NAT-03
  - SES-02
  - SET-02
tags:
  - bug-fix
  - lint
  - cross-platform
  - i18n
must_haves:
  truths:
    - "Message chevron (NAV-A3 / CONCERNS H13) is visible on iOS, Android, AND on web compact form factor — not only when a pointer hovers"
    - "The resize handle (NAT-03 / CONCERNS C12) does not crash on native iOS/Android — onPointerEnter/onPointerLeave are gated by isWeb or moved into a .web.tsx variant"
    - "OpenCode listPersistedAgents (SES-02 / CONCERNS H4) returns recovered sessions after a daemon restart, not always [] — verified by a vitest test"
    - "chromeEnabled (SET-02 / CONCERNS C11) is split into chromeLayoutEnabled and keyboardShortcutsEnabled with the SAME effective values existing users see today (zero behavior change)"
    - "Two warn-level lints are wired into npm: lint:hover (catches isHovered-alone visibility gates) and lint:pointer-events (catches onPointerEnter/onPointerLeave outside .web.* without isWeb gate). Both have counter-baselines."
  artifacts:
    - path: "packages/app/src/components/message.tsx"
      provides: "Chevron visibility uses isHovered || isExpanded || isNative || isCompact (NAV-A3)"
      contains: "isNative || isCompact"
    - path: "packages/app/src/components/resize-handle.tsx"
      provides: "Pointer events gated by isWeb (NAT-03)"
      contains: "isWeb ? handlePointerEnter"
    - path: "packages/server/src/server/agent/providers/opencode-agent.ts"
      provides: "listPersistedAgents returns recovered descriptors mirroring claude-agent.ts pattern"
      contains: "collectRecentOpenCodeSessions"
    - path: "packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts"
      provides: "Vitest coverage for SES-02 fix"
      contains: 'describe("OpenCode listPersistedAgents'
    - path: "packages/app/src/app/_layout.tsx"
      provides: "chromeLayoutEnabled + keyboardShortcutsEnabled split derivations preserving today's effective values (SET-02)"
      contains: "chromeLayoutEnabled"
    - path: "packages/app/src/i18n/locales/en.json"
      provides: "English strings for any new visible copy from this plan"
    - path: "packages/app/src/i18n/locales/zh.json"
      provides: "Chinese parity for the same keys (CLAUDE.md hard rule)"
    - path: "tools/lint/is-hovered-alone.ts"
      provides: "Warn-level lint for isHovered-alone visibility gates"
    - path: "tools/lint/pointer-events-web-only.ts"
      provides: "Warn-level lint for onPointerEnter/onPointerLeave outside .web.* without isWeb gate"
  key_links:
    - from: "packages/app/src/components/message.tsx (chevron isActive)"
      to: "@/constants/platform isNative + useIsCompactFormFactor"
      via: "import + JSX expression"
      pattern: "isHovered \\|\\| isExpanded \\|\\| isNative \\|\\| isCompact"
    - from: "packages/server/src/server/agent/providers/opencode-agent.ts listPersistedAgents"
      to: "OpenCode session storage filesystem"
      via: "fs.readdir / fs.stat / JSONL parse"
      pattern: "collectRecentOpenCodeSessions"
    - from: "packages/app/src/app/_layout.tsx"
      to: "renamed flags chromeLayoutEnabled + keyboardShortcutsEnabled"
      via: "destructured props + derivations"
      pattern: "chromeLayoutEnabled"
---

<objective>
Close the four shipped regressions Phase 1 owns (CONCERNS H13, C12, H4, C11), AND ship the two warn-level lints that prevent these patterns from regressing in subsequent phases. Bug fixes are atomic per concern (per CONTEXT.md `<deferred>` "atomic-per-bug for clean blame").

Per ROADMAP P1 success criteria #4 and #5:

- NAV-A3 — chevron visible via `isHovered || isNative || isCompact` + warn-level lint blocking `isHovered`-alone visibility gates
- NAT-03 — resize handle no longer uses `onPointerEnter`/`onPointerLeave` outside .web.tsx without an isWeb guard + warn-level lint
- SES-02 — OpenCode `listPersistedAgents` returns recovered sessions after daemon restart (vitest-verifiable; UAT happens in execution time)
- SET-02 — `chromeEnabled` split into `chromeLayoutEnabled` + `keyboardShortcutsEnabled` with existing-user behavior preserved

Both lints ship at warn-level (per D-10) and are promoted to error in Phase 5. Counter-baselines guarantee the warn count never increases.

Out of scope for this plan (per CONTEXT.md `<deferred>`):

- OpenCode "Recovered N sessions" toast — Phase 4 polish
- Lint-rule-promotion-to-error — Phase 5
- Sweeping the other onPointerEnter callsites cited in 01-PATTERNS.md (sidebar-workspace-list, terminal-emulator, web-desktop-scrollbar, tooltip, workspace-hover-card) — those are warn-level lint coverage, NOT in-scope fixes; only the resize-handle (cited regression) is fixed here
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-CONTEXT.md
@.planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@docs/CODING_STANDARDS.md

<interfaces>

NAV-A3 chevron fix (message.tsx around line 2601):

- BEFORE: `const isActive = isHovered || isExpanded;`
- AFTER: `const isActive = isHovered || isExpanded || isNative || isCompact;`
- Required imports added at top of file: `import { isNative } from "@/constants/platform";` (the file likely already imports `useIsCompactFormFactor` for the in-file analog at line 404 — verify; otherwise add `import { useIsCompactFormFactor } from "@/constants/layout";`).
- The in-file canonical analog at message.tsx:404 (`const showCopyButton = hasText && (isCompact || messageHovered || copyButtonHovered);`) already uses the pattern; the chevron isActive expression is the bug being closed.

NAT-03 pointer-events fix (resize-handle.tsx lines 138-148):

- BEFORE: `onPointerEnter={handlePointerEnter} onPointerLeave={handlePointerLeave}`
- AFTER: `onPointerEnter={isWeb ? handlePointerEnter : undefined} onPointerLeave={isWeb ? handlePointerLeave : undefined}`
- Required import: `import { isWeb } from "@/constants/platform";`
- Analog: toast-host.tsx:257-258 (`onPointerEnter={isWeb ? pauseDismiss : undefined}`).
- Decision: keep as inline gate (lighter touch — the rest of the component is cross-platform). A `.web.tsx` split is overkill for a 2-line gate.

SES-02 OpenCode listPersistedAgents (opencode-agent.ts:1171-1176):

- BEFORE: stub returning `[]`.
- AFTER: mirror claude-agent.ts:1190-1206 — resolve OpenCode session storage root, mirror the `pathExists → collect → parse → filter → slice(limit)` pipeline. Co-locate `collectRecentOpenCodeSessions` and `parseOpenCodeSessionDescriptor` helpers at the bottom of the file (matching Claude organization).
- OpenCode session location: discovered via grep against the opencode-agent.ts code paths that WRITE sessions (look for OpenCodeServerManager, session-write code paths, and any reference to `~/.opencode/` or similar). If discovery is ambiguous, planner notes the resolved path in the SUMMARY for downstream agents.

SET-02 chromeEnabled split (\_layout.tsx:425-562, 854):

- BEFORE: prop `chromeEnabled?: boolean`; derivation `const chromeEnabled = chromeEnabledOverride ?? daemons.length > 0;`; downstream `keyboardShortcutsEnabled = chromeEnabled || pathname.startsWith("/settings");`.
- AFTER: prop renamed to `chromeLayoutEnabled?: boolean` (source props at line 854 also renamed); two named derivations:
  - `const chromeLayoutEnabled = chromeLayoutEnabledOverride ?? daemons.length > 0;` (was `chromeEnabled`)
  - `const keyboardShortcutsEnabled = chromeLayoutEnabled || pathname.startsWith("/settings");` (unchanged formula, renamed dependency)
- Every downstream consumer (lines 504, 505, 510, 538, 562) reads from `chromeLayoutEnabled` instead of `chromeEnabled`.
- The root caller at line 854 also passes `chromeLayoutEnabled={shouldShowAppChrome}` (same value, renamed prop).
- D-06 migration ("both new flags inherit the existing chromeEnabled value on first launch"): in the current code there is NO persisted store — chromeEnabled is a derivation. Therefore "first-launch migration preserving existing values" reduces to "the new derivations produce the SAME effective values as the old code did for existing users." The TODO comment at lines 479-482 directly motivates this split — keep that comment as the in-code rationale, updated to reflect the split is now implemented.
- TODO comment at \_layout.tsx:479-482 is REMOVED after the split (or replaced with a reference to the in-code split that closes it).
- D-08 schema annotation: `chromeEnabled` does NOT live in any Zod schema in messages.ts (verified by grep — it's a UI prop only). Therefore D-08's `@deprecated since=v1.11 removeAfter=v1.16` annotation is N/A for messages.ts in this phase. If a Zod schema is later added for the flag (e.g. recents broadcast), the annotation discipline from Plan 01-01 applies. Plan SUMMARY notes this finding for Phase 5's deprecation-removal milestone.

Two new lint scripts (mirror Plan 01-01 / Plan 01-02 lint shape):

- tools/lint/is-hovered-alone.ts: scans .ts/.tsx under packages/app/src/, excluding tests. Detects any `isHovered` token that appears in a JSX visibility gate (e.g. inside `&&`, `?`, in `style={[..., isHovered && ...]}`, in render-conditional `{isHovered && <X/>}`) WITHOUT being adjacent to `isNative`, `isCompact`, or being inside a .web.\* file. Emits WARN, exits 0 (warn-only) unless count > baseline. Baseline written via `--write-baseline` after Task 1.
- tools/lint/pointer-events-web-only.ts: scans .ts/.tsx under packages/app/src/, EXCLUDING .web._ files (those are allowed any DOM API). Detects `onPointerEnter=` or `onPointerLeave=` JSX props that are NOT `={isWeb ? ... : undefined}` and NOT inside a .web._ file. Emits WARN, same baseline mechanism.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NAV-A3 chevron fix in message.tsx + warn-level isHovered-alone lint</name>
  <files>
    packages/app/src/components/message.tsx,
    tools/lint/is-hovered-alone.ts,
    tools/lint/is-hovered-alone.test.ts,
    tools/lint/is-hovered-alone.baseline.json,
    package.json
  </files>
  <read_first>
    - packages/app/src/components/message.tsx (lines 400-420 for the in-file analog at messageHovered, then lines 2596-2620 for the buggy chevron site)
    - packages/app/src/constants/platform.ts (verify isNative export shape)
    - packages/app/src/constants/layout.ts (verify useIsCompactFormFactor export shape)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"`packages/app/src/components/message.tsx` (MODIFIED) — H13 chevron fix" — the canonical hover-fallback expression
    - CLAUDE.md "Hover only works on web" — context for why isHovered alone is broken on native
    - tools/lint/hardcoded-color.ts (Plan 01-02 Task 3) — mirror its tsx-based shape, exit-code conventions, baseline mechanism
  </read_first>
  <behavior>
    - In packages/app/src/components/message.tsx, the chevron `isActive` derivation around line 2601 is rewritten to `isHovered || isExpanded || isNative || isCompact`.
    - Required imports are present at the top of message.tsx: `isNative` from `@/constants/platform`, and `useIsCompactFormFactor` (or `isCompact` if already in scope from the messageHovered analog). The expression uses `isCompact` (the local variable), NOT `useIsCompactFormFactor()` inline.
    - Visual on iOS / Android / web compact form factor: chevron is always rendered as if hovered (the affordance is always visible because hover doesn't fire on native).
    - The lint script is wired as `npm run lint:hover`; baseline is captured AFTER the message.tsx fix lands so that fix doesn't contribute to the baseline.
  </behavior>
  <action>
    1. Open message.tsx. Read lines 400-420 — confirm `useIsCompactFormFactor` is imported and `isCompact` is in scope (per the in-file analog). If `isNative` is not imported, add `import { isNative } from "@/constants/platform";` to the existing imports block at the top.
    2. Locate the buggy line: `const isActive = isHovered || isExpanded;` (line ~2601). Verify `isCompact` is in scope at this position (it's likely declared once at the component-function top level around line 404; if the chevron lives inside a different sub-component, declare `const isCompact = useIsCompactFormFactor();` at the start of that sub-component).
    3. Rewrite to: `const isActive = isHovered || isExpanded || isNative || isCompact;`
    4. Run `npm run typecheck` (the file is large — typecheck is the deterministic acceptance for this surgical edit). Run `npm run lint -- packages/app/src/components/message.tsx`.
    5. Create tools/lint/is-hovered-alone.ts. Same shape as Plan 01-02 Task 3's hardcoded-color lint:
       - Walk packages/app/src/ for .ts/.tsx EXCLUDING `*.test.tsx`, `*.test.ts`, and `*.web.*`.
       - Per file, scan line-by-line. A line is a violation if it contains the token `isHovered` AND appears inside a visibility gate (heuristic regex catching: `isHovered\s*&&`, `\?\s*isHovered`, `isHovered\s*\?`, `&&\s*isHovered`) AND the line does NOT also contain `isNative` or `isCompact`.
       - Naive: a sequence of consecutive lines forming the same JSX expression should be considered together (look back/forward up to 3 lines for `isNative`/`isCompact` adjacency before flagging).
       - Emit `WARN  {file}:{line}  isHovered used alone in visibility gate — combine with isNative or isCompact (CLAUDE.md "Hover only works on web")` to stderr.
       - Same `--write-baseline` and counter-comparison mechanism as the hardcoded-color lint.
       - Include the `// PHASE 5: tighten — exit 1 on ANY violation per ROADMAP P5` cleanup marker.
    6. Create tools/lint/is-hovered-alone.test.ts covering: (a) `isHovered && <X/>` alone -> violation, (b) `isHovered || isNative` -> no violation, (c) `isHovered || isCompact` -> no violation, (d) `*.web.tsx` file with `isHovered` alone -> no violation (excluded), (e) `isHovered` inside string literal -> no violation, (f) baseline counter mechanism (count == baseline -> 0; count > baseline -> 1).
    7. Add to package.json scripts: `"lint:hover": "tsx tools/lint/is-hovered-alone.ts"` and `"lint:hover:baseline": "tsx tools/lint/is-hovered-alone.ts --write-baseline"`.
    8. Run `npx tsx tools/lint/is-hovered-alone.ts --write-baseline` to capture the post-fix baseline. Commit `tools/lint/is-hovered-alone.baseline.json`.
    9. Run `npm run lint:hover` (must exit 0).
    10. Run `npx vitest run tools/lint/is-hovered-alone.test.ts --bail=1` (must exit 0).
    11. Run `npm run format -- packages/app/src/components/message.tsx`.
  </action>
  <verify>
    <automated>grep -n "isHovered || isExpanded || isNative || isCompact" packages/app/src/components/message.tsx && npm run typecheck 2>&1 | tail -3 && npm run lint:hover 2>&1 | tail -5 && npx vitest run tools/lint/is-hovered-alone.test.ts --bail=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isHovered || isExpanded || isNative || isCompact" packages/app/src/components/message.tsx` returns at least `1`
    - `grep -c "import { isNative" packages/app/src/components/message.tsx` returns at least `1`
    - `test -f tools/lint/is-hovered-alone.ts` exits 0
    - `test -f tools/lint/is-hovered-alone.test.ts` exits 0
    - `test -f tools/lint/is-hovered-alone.baseline.json` exits 0
    - `grep -c "lint:hover" package.json` returns at least `2`
    - `grep -c "PHASE 5: tighten" tools/lint/is-hovered-alone.ts` returns at least `1`
    - `npm run typecheck` exits 0
    - `npm run lint -- packages/app/src/components/message.tsx` exits 0
    - `npm run lint:hover` exits 0
    - `npx vitest run tools/lint/is-hovered-alone.test.ts --bail=1` exits 0
  </acceptance_criteria>
  <done>Chevron visible on native + compact form factor; isHovered-alone lint enforced at warn-level with counter-baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NAT-03 resize-handle pointer-events fix + warn-level pointer-events-web-only lint</name>
  <files>
    packages/app/src/components/resize-handle.tsx,
    tools/lint/pointer-events-web-only.ts,
    tools/lint/pointer-events-web-only.test.ts,
    tools/lint/pointer-events-web-only.baseline.json,
    package.json
  </files>
  <read_first>
    - packages/app/src/components/resize-handle.tsx (lines 130-152 — the buggy onPointerEnter/onPointerLeave site)
    - packages/app/src/components/toast-host.tsx (lines 250-270 — canonical safe analog: `onPointerEnter={isWeb ? pauseDismiss : undefined}`)
    - packages/app/src/constants/platform.ts (verify isWeb export)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"`packages/app/src/components/resize-handle.tsx` (MODIFIED) — C12 pointer events fix"
    - CLAUDE.md "NEVER use onPointerEnter/onPointerLeave. They don't fire on native iOS." — the rule the lint enforces
    - tools/lint/is-hovered-alone.ts (Task 1 of this plan) — mirror exact shape and baseline mechanism
  </read_first>
  <behavior>
    - resize-handle.tsx lines 145-148 are gated by `isWeb`: `onPointerEnter={isWeb ? handlePointerEnter : undefined}` and `onPointerLeave={isWeb ? handlePointerLeave : undefined}`.
    - `import { isWeb } from "@/constants/platform";` is added if not already present.
    - `onPointerDown` is NOT gated — it fires correctly on native (it's the touch-equivalent gesture entry; CLAUDE.md only flags `onPointerEnter`/`onPointerLeave` as native-broken).
    - On native iOS/Android, the handle no longer crashes (the bug being closed). On web, behavior is unchanged.
    - The lint script `lint:pointer-events` flags onPointerEnter/onPointerLeave outside .web.* without isWeb gate; baseline is captured AFTER the fix lands.
  </behavior>
  <action>
    1. Open packages/app/src/components/resize-handle.tsx. Add `import { isWeb } from "@/constants/platform";` to the imports block if not already present.
    2. Modify lines 146-147:
       - `onPointerEnter={handlePointerEnter}` -> `onPointerEnter={isWeb ? handlePointerEnter : undefined}`
       - `onPointerLeave={handlePointerLeave}` -> `onPointerLeave={isWeb ? handlePointerLeave : undefined}`
    3. Leave `onPointerDown={handlePointerDown}` untouched (per <behavior>).
    4. Run `npm run typecheck` and `npm run lint -- packages/app/src/components/resize-handle.tsx`.
    5. Create tools/lint/pointer-events-web-only.ts. Same shape as Task 1's lint:
       - Walk packages/app/src/ for .ts/.tsx EXCLUDING `*.web.tsx`, `*.web.ts`, `*.test.tsx`, `*.test.ts`.
       - Per file, scan line-by-line. A line is a violation if it contains `onPointerEnter=` or `onPointerLeave=` AND the assigned expression does NOT match `isWeb\s*\?` AND the file path is not under `.web.`.
       - Emit `WARN  {file}:{line}  onPointerEnter/onPointerLeave used outside .web.* without isWeb gate (CLAUDE.md "NEVER use onPointerEnter/onPointerLeave")` to stderr.
       - Same `--write-baseline` and counter-comparison mechanism.
       - Include the `// PHASE 5: tighten — exit 1 on ANY violation per ROADMAP P5` cleanup marker.
    6. Create tools/lint/pointer-events-web-only.test.ts covering: (a) `onPointerEnter={fn}` -> violation, (b) `onPointerEnter={isWeb ? fn : undefined}` -> no violation, (c) file under `*.web.tsx` with raw `onPointerEnter={fn}` -> no violation (excluded), (d) `onPointerDown={fn}` -> no violation (the rule only catches Enter/Leave), (e) baseline counter mechanism.
    7. Add to package.json scripts: `"lint:pointer-events": "tsx tools/lint/pointer-events-web-only.ts"` and `"lint:pointer-events:baseline": "tsx tools/lint/pointer-events-web-only.ts --write-baseline"`.
    8. Run `npx tsx tools/lint/pointer-events-web-only.ts --write-baseline` to capture the post-fix baseline. Commit `tools/lint/pointer-events-web-only.baseline.json`. Note: the baseline will be > 0 because 01-PATTERNS.md cites multiple OUT-OF-SCOPE callsites (sidebar-workspace-list, terminal-emulator, web-desktop-scrollbar, tooltip, workspace-hover-card). Those are tracked as pre-existing warn-level coverage; Phase 5 promotion to error will require sweeping them. The SUMMARY records the baseline count and the file list.
    9. Run `npm run lint:pointer-events` (must exit 0 against baseline).
    10. Run `npx vitest run tools/lint/pointer-events-web-only.test.ts --bail=1` (must exit 0).
    11. Run `npm run format -- packages/app/src/components/resize-handle.tsx`.
  </action>
  <verify>
    <automated>grep -n "isWeb ? handlePointerEnter" packages/app/src/components/resize-handle.tsx && grep -n "isWeb ? handlePointerLeave" packages/app/src/components/resize-handle.tsx && npm run typecheck 2>&1 | tail -3 && npm run lint:pointer-events 2>&1 | tail -5 && npx vitest run tools/lint/pointer-events-web-only.test.ts --bail=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isWeb ? handlePointerEnter" packages/app/src/components/resize-handle.tsx` returns at least `1`
    - `grep -c "isWeb ? handlePointerLeave" packages/app/src/components/resize-handle.tsx` returns at least `1`
    - `grep -c "import { isWeb" packages/app/src/components/resize-handle.tsx` returns at least `1`
    - `test -f tools/lint/pointer-events-web-only.ts` exits 0
    - `test -f tools/lint/pointer-events-web-only.test.ts` exits 0
    - `test -f tools/lint/pointer-events-web-only.baseline.json` exits 0
    - `grep -c "lint:pointer-events" package.json` returns at least `2`
    - `grep -c "PHASE 5: tighten" tools/lint/pointer-events-web-only.ts` returns at least `1`
    - `npm run typecheck` exits 0
    - `npm run lint -- packages/app/src/components/resize-handle.tsx` exits 0
    - `npm run lint:pointer-events` exits 0
    - `npx vitest run tools/lint/pointer-events-web-only.test.ts --bail=1` exits 0
  </acceptance_criteria>
  <done>Resize handle no longer crashes on native; pointer-events lint enforced at warn-level with counter-baseline (carries the out-of-scope sweep as warn-level coverage for Phase 5 promotion).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SES-02 OpenCode listPersistedAgents implementation + SET-02 chromeEnabled split</name>
  <files>
    packages/server/src/server/agent/providers/opencode-agent.ts,
    packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts,
    packages/app/src/app/_layout.tsx,
    packages/app/src/i18n/locales/en.json,
    packages/app/src/i18n/locales/zh.json
  </files>
  <read_first>
    - packages/server/src/server/agent/providers/opencode-agent.ts (lines 1170-1180 for the stub; ALSO grep this file for "OpenCodeServerManager" or "session" references that hint at the storage path — the planner must discover where OpenCode persists sessions before writing the implementation)
    - packages/server/src/server/agent/providers/claude-agent.ts (lines 1190-1206 for listPersistedAgents pattern; lines 4346, 4355, 4446 for pathExists, collectRecentClaudeSessions, parseClaudeSessionDescriptor helpers)
    - packages/server/src/server/agent/providers/codex-app-server-agent.spawn-error.test.ts (lines 30-50 for the listPersistedAgents test shape)
    - packages/app/src/app/_layout.tsx (lines 425-565 + 854 — the chromeEnabled prop, derivation, downstream consumers, and the root caller)
    - .planning/phases/01-architectural-foundations-gating-bug-fixes/01-PATTERNS.md §"opencode-agent.ts (MODIFIED) — H4 fix" + §"Flag store under stores/ — chromeEnabled split"
    - .planning/codebase/ARCHITECTURE.md "Agent Providers" table — confirms OpenCode is "Provider-managed" session storage
    - CLAUDE.md "NEVER make breaking changes to WebSocket or message schemas" + "Bilingual" — every visible string change touches both locales
  </read_first>
  <behavior>
    SES-02:
    - `opencode-agent.ts listPersistedAgents` returns an array of `PersistedAgentDescriptor` matching the same shape Claude returns. Empty array only if storage is genuinely empty or missing — NEVER as a default-stub.
    - Storage discovery: planner greps the file for OpenCode's session-write code path. Likely candidates: `~/.opencode/sessions/`, `OPENCODE_HOME`, or a path embedded in OpenCodeServerManager. Document the resolved path in the SUMMARY.
    - Helpers `collectRecentOpenCodeSessions` and `parseOpenCodeSessionDescriptor` are co-located at the bottom of opencode-agent.ts mirroring Claude organization.
    - Vitest test (opencode-agent.list-persisted.test.ts): creates a temp directory acting as a fake OpenCode storage root, writes one or two synthetic JSONL session files, invokes `listPersistedAgents` with the storage root injected (or via env var if no DI seam exists), asserts at least one descriptor is returned with the expected fields. Mirrors codex-app-server-agent.spawn-error.test.ts shape.
    - Per PITFALLS Integration Gotchas: malformed JSONL files are logged + skipped (Claude pattern: `.filter(d => d !== null)`), NOT thrown out of listPersistedAgents.
    - SES-02 UAT acceptance ("kill daemon mid-session, restart, sessions appear in recents") happens at execution time; Plan SUMMARY notes the test as the automated proxy.

    SET-02:
    - Prop `chromeEnabled?: boolean` renamed to `chromeLayoutEnabled?: boolean` everywhere it appears in _layout.tsx (declarations at lines 429, 437, 543, 546).
    - Derivation `const chromeEnabled = chromeEnabledOverride ?? daemons.length > 0;` renamed to `const chromeLayoutEnabled = chromeLayoutEnabledOverride ?? daemons.length > 0;`.
    - `keyboardShortcutsEnabled` derivation keeps its formula but now reads `chromeLayoutEnabled`: `const keyboardShortcutsEnabled = chromeLayoutEnabled || pathname.startsWith("/settings");`.
    - Downstream consumers at lines 504, 505, 510, 538, 562 read `chromeLayoutEnabled` (the rendered chrome — sidebars, gesture wrapper).
    - Root caller at line 854: `chromeEnabled={shouldShowAppChrome}` -> `chromeLayoutEnabled={shouldShowAppChrome}` (same value, renamed prop).
    - Pre-split TODO comment at lines 479-482 is replaced with an in-code rationale: `// chromeLayoutEnabled gates rendered chrome (sidebars, mobile gesture). keyboardShortcutsEnabled is its own derivation that ALSO enables shortcuts on /settings — split per SET-02 / CONCERNS C11.`
    - D-06 "preserve existing values" check: the new derivations produce IDENTICAL effective values to the pre-split code for every input — verified by grepping that `chromeLayoutEnabled = chromeLayoutEnabledOverride ?? daemons.length > 0` and `keyboardShortcutsEnabled = chromeLayoutEnabled || pathname.startsWith("/settings")` are the EXACT same expressions as the pre-split right-hand sides.
    - D-08 schema annotation: SUMMARY notes that grep of `messages.ts` for `chromeEnabled` returns ZERO matches — there is no Zod schema field to annotate. The deprecation calendar applies only when a Zod schema field exists. Phase 5's deprecation-removal milestone owns the cleanup if a schema is later added.
    - i18n: this task adds NO new visible strings (the rename is purely internal — both flags are derivations, not user-visible toggles in this phase). en.json and zh.json are touched only if the linter shows a missing-key violation (no expected changes). If new strings are needed for any reason, they MUST be added to BOTH locales in the same commit (CLAUDE.md hard rule).

  </behavior>
  <action>
    1. **SES-02 (sub-task A):** Open opencode-agent.ts. Grep for "session" and "storage" within the file to identify where OpenCode sessions persist (`grep -n "sessions\|storage\|HOME\|directory\|opencode" packages/server/src/server/agent/providers/opencode-agent.ts | head -40`). If unambiguous, proceed; if ambiguous, also grep the OpenCode upstream npm package's `package.json` and README for the storage location. Document the resolved path in the SUMMARY.
    2. Read claude-agent.ts:1190-1206, 4346, 4355, 4446 (the listPersistedAgents impl + helpers).
    3. Replace the opencode-agent.ts:1171-1176 stub with a real implementation that mirrors Claude's pipeline: resolve storage root → pathExists check → collect recent N candidates → parse each into a descriptor → filter nulls → slice(limit). Inject the storage root via env var (`OTTIE_OPENCODE_HOME` for test override; default to whatever the natural OpenCode home is) so the test can substitute a temp dir.
    4. Add `collectRecentOpenCodeSessions` and `parseOpenCodeSessionDescriptor` helpers at the BOTTOM of opencode-agent.ts. Keep them file-local (not exported) — same as Claude.
    5. Create opencode-agent.list-persisted.test.ts. Fixture: `mkdtempSync` a temp dir, write one or two synthetic JSONL files matching whatever shape `parseOpenCodeSessionDescriptor` expects (the planner choses a minimal valid shape based on what `PersistedAgentDescriptor` requires). Set `OTTIE_OPENCODE_HOME` to the temp dir, call `listPersistedAgents({limit: 5})`, assert `result.length >= 1` and `result[0]` has the expected fields. Add a second test case: malformed JSONL is logged + skipped (the result still parses, just excludes the bad entry).
    6. Run `npx vitest run packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts --bail=1` (must exit 0).
    7. Run `npm run typecheck` and `npm run lint -- packages/server/src/server/agent/providers/opencode-agent.ts`.

    8. **SET-02 (sub-task B):** Open _layout.tsx. Use a single sed-equivalent edit pass (via the Edit tool, NOT actual sed — Bash sed is forbidden by tool conventions) to rename:
       - `chromeEnabled?: boolean;` → `chromeLayoutEnabled?: boolean;` (line 429, 546)
       - `chromeEnabled: chromeEnabledOverride,` → `chromeLayoutEnabled: chromeLayoutEnabledOverride,` (line 437)
       - `chromeEnabled,` (function param at line 543) → `chromeLayoutEnabled,`
       - `chromeEnabled: boolean;` (line 546) → `chromeLayoutEnabled: boolean;`
       - `const chromeEnabled = chromeEnabledOverride ?? daemons.length > 0;` (line 457) → `const chromeLayoutEnabled = chromeLayoutEnabledOverride ?? daemons.length > 0;`
       - `chromeEnabled || pathname.startsWith("/settings")` (line 483) → `chromeLayoutEnabled || pathname.startsWith("/settings")`
       - All downstream uses on lines 504, 505, 510, 538, 562: `chromeEnabled` → `chromeLayoutEnabled`
       - Root caller line 854: `chromeEnabled={shouldShowAppChrome}` → `chromeLayoutEnabled={shouldShowAppChrome}`
    9. Replace the TODO comment at lines 479-482 with: `// chromeLayoutEnabled gates rendered chrome (sidebars, mobile gesture). keyboardShortcutsEnabled is its own derivation that ALSO enables shortcuts on /settings — split per SET-02 / CONCERNS C11.`
    10. Run `npm run typecheck` (catches every missed rename — TS errors fail loudly because the type changed).
    11. Run `npm run lint -- packages/app/src/app/_layout.tsx`.
    12. Verify via grep that `chromeEnabled` (the OLD name) appears ZERO times in _layout.tsx after the edit.
    13. Run `npm run format -- packages/server/src/server/agent/providers/opencode-agent.ts packages/app/src/app/_layout.tsx`.

  </action>
  <verify>
    <automated>! grep -q "chromeEnabled" packages/app/src/app/_layout.tsx && grep -c "chromeLayoutEnabled" packages/app/src/app/_layout.tsx | awk '{print ($1 >= 8)}' && ! grep -A2 "async listPersistedAgents" packages/server/src/server/agent/providers/opencode-agent.ts | grep -q "return \[\];" && grep -q "collectRecentOpenCodeSessions" packages/server/src/server/agent/providers/opencode-agent.ts && npx vitest run packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts --bail=1 2>&1 | tail -5 && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chromeEnabled" packages/app/src/app/_layout.tsx` returns `0` (every callsite renamed)
    - `grep -c "chromeLayoutEnabled" packages/app/src/app/_layout.tsx` returns at least `8` (matches the 8+ callsites in the original file)
    - `grep -c "keyboardShortcutsEnabled" packages/app/src/app/_layout.tsx` returns at least `2` (derivation + consumer — preserved unchanged in formula)
    - `grep -c "split per SET-02 / CONCERNS C11" packages/app/src/app/_layout.tsx` returns `1` (TODO replaced with in-code rationale)
    - `grep -A3 "async listPersistedAgents" packages/server/src/server/agent/providers/opencode-agent.ts | grep -c "return \[\];" | awk '{print ($1 == 0)}'` returns `1` (stub replaced — return [] no longer the immediate body)
    - `grep -c "collectRecentOpenCodeSessions" packages/server/src/server/agent/providers/opencode-agent.ts` returns at least `2` (helper + caller)
    - `grep -c "parseOpenCodeSessionDescriptor" packages/server/src/server/agent/providers/opencode-agent.ts` returns at least `2` (helper + caller)
    - `test -f packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts` exits 0
    - `npx vitest run packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts --bail=1` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint -- packages/app/src/app/_layout.tsx packages/server/src/server/agent/providers/opencode-agent.ts` exits 0
    - `diff <(grep -E "(chromeEnabled|chromeLayoutEnabled)" packages/app/src/app/_layout.tsx | grep -c chromeEnabled) <(echo 0)` exits 0 (zero old-name leakage)
  </acceptance_criteria>
  <done>OpenCode listPersistedAgents returns recovered descriptors via Claude-mirrored pipeline (vitest-verified); chromeEnabled split into chromeLayoutEnabled + keyboardShortcutsEnabled with identical effective values; both fixes typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                     |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| OpenCode session JSONL files → daemon process | Filesystem input crosses into JSON.parse; malformed files could throw or expose memory pressure |
| \_layout.tsx prop passing                     | Internal — type-narrowed at compile time                                                        |
| Lint scripts → CI pipeline                    | Same as Plans 01-01 / 01-02                                                                     |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                |
| --------- | -------------------------- | --------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | T (Tampering)              | OpenCode JSONL files could be hand-crafted to inject malicious data into PersistedAgentDescriptor   | mitigate    | parseOpenCodeSessionDescriptor returns null on parse failure (Claude-mirrored pattern); .filter excludes nulls. PersistedAgentDescriptor is a structural type — no code execution path from descriptor fields. |
| T-03-02   | D (DoS)                    | A directory with thousands of JSONL files could slow listPersistedAgents                            | mitigate    | Mirror Claude's `slice(0, limit * 3)` candidate cap before parsing; `slice(0, limit)` after parsing. Bounded work by design.                                                                                   |
| T-03-03   | I (Information disclosure) | Error logs from malformed JSONL might include path that reveals home directory layout               | accept      | Pino logger already redacts user-home patterns where present; this is a low-value leak (user's own machine). Risk: low.                                                                                        |
| T-03-04   | E (Elevation)              | resize-handle pointer-events fix could not be elevation; isHovered chevron fix is purely UI         | accept      | No new attack surface in NAV-A3 / NAT-03 — both are crash/visibility fixes.                                                                                                                                    |
| T-03-05   | R (Repudiation)            | chromeLayoutEnabled rename across files might miss callsites, leading to runtime "undefined" errors | mitigate    | TS type system enforces — the prop type narrows at compile time, so any missed callsite fails `npm run typecheck`. Acceptance criteria explicitly gate on typecheck success.                                   |

**Note:** This plan ships zero new attack surface. The OpenCode pipeline reads filesystem files the same way Claude already does (mirrored pattern); the chevron and pointer-events fixes are bug closures, not new code paths; the chromeEnabled rename is internal-only.
</threat_model>

<verification>
1. `npm run typecheck` exits 0.
2. `npm run lint` exits 0 (broad lint, all touched files clean).
3. `npm run lint:hover` exits 0 (NAV-A3 baseline guard).
4. `npm run lint:pointer-events` exits 0 (NAT-03 baseline guard).
5. `npx vitest run packages/server/src/server/agent/providers/opencode-agent.list-persisted.test.ts tools/lint/is-hovered-alone.test.ts tools/lint/pointer-events-web-only.test.ts --bail=1` exits 0.
6. `grep -c "chromeEnabled" packages/app/src/app/_layout.tsx` returns 0.
7. `grep -c "isHovered || isExpanded" packages/app/src/components/message.tsx | awk '{print ($1 == 0)}'` returns 1 (the buggy expression no longer exists in any form not followed by `|| isNative || isCompact`).
8. `grep -c "isWeb ? handlePointerEnter" packages/app/src/components/resize-handle.tsx` returns at least 1.
9. The two new lint baseline files exist and are committed: `tools/lint/is-hovered-alone.baseline.json`, `tools/lint/pointer-events-web-only.baseline.json`.
</verification>

<success_criteria>

- NAV-A3 closed: chevron visible on iOS / Android / web compact; isHovered-alone lint at warn-level
- NAT-03 closed: resize handle pointer events gated by isWeb; pointer-events-web-only lint at warn-level (out-of-scope sweeps tracked in baseline for Phase 5 promotion)
- SES-02 closed: OpenCode listPersistedAgents returns recovered descriptors; vitest test verifies; UAT-eligible at execution
- SET-02 closed: chromeEnabled split into chromeLayoutEnabled + keyboardShortcutsEnabled; effective values identical for existing users; TODO replaced with in-code rationale
- Three lint scripts wired (`lint:hover`, `lint:pointer-events` from this plan; `lint:colors` from Plan 02; `lint:schema` from Plan 01) — all exit 0; counter-baselines committed
- All checks green: typecheck, lint, three new lint:\* commands, the new vitest tests
- Bilingual i18n parity preserved (this plan adds no new visible strings, but any unexpected addition would touch both en.json and zh.json)
  </success_criteria>

<output>
After completion, create .planning/phases/01-architectural-foundations-gating-bug-fixes/01-03-SUMMARY.md capturing:
- Files created / modified
- The OpenCode session storage path discovered (so Phase 4 polish work can write the "Recovered N sessions" toast against the right surface)
- The pointer-events lint baseline count + the file list contributing to it (so Phase 5 promotion has the sweep target)
- The isHovered-alone lint baseline count + the file list (same)
- Confirmation that grep of messages.ts for `chromeEnabled` returns 0 — D-08 schema annotation does NOT apply this phase
- Any deviations from the plan and why
</output>
