# @ottie/desktop

Tauri v2 desktop shell for Ottie. Loads the Expo web build from `packages/app/dist` and supervises the daemon as a sidecar binary.

```bash
pnpm dev    # tauri dev
pnpm build  # tauri build
```
